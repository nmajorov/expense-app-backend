apiVersion: v1
kind: Service
metadata:
  name: expense-app-backend
  labels:
    app: expense-app-backend
spec:
  type: NodePort
  selector:
    app: expense-app-backend
  ports:
    - name: http
      port: 7000
      targetPort: http
      nodePort: 30070
