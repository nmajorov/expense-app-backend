apiVersion: v1
kind: Service
metadata:
  name: expense-app-backend
  labels:
    app: expense-app-backend
spec:
  type: ClusterIP
  selector:
    app: expense-app-backend
  ports:
    - name: http
      port: 7000
      targetPort: http
